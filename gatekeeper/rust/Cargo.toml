[workspace]
members = [
  "ta",
  "tests",
  "wire",
]
resolver = "2"

[patch.crates-io]
gk-ta = { path = "ta" }
gk-tests = { path = "tests" }
gk-wire = { path = "wire" }
hal-wire = { path = "../../security/hals/wire" }
hal-wire-derive = { path = "../../security/hals/derive" }
